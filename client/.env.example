# Copy this to client/.env.production (ignored by git) before running sync-portfolio.sh
# Only needed when the frontend is deployed separately from the backend (e.g. joavn.dev/versed + Railway)

# Railway backend URL
VITE_SERVER_URL=https://your-railway-url.up.railway.app
